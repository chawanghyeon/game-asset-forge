# Game Asset Forge Viewer 작업 지침

현재 저장소 범위는 `viewer/`의 웹 GLB 뷰어다. 이전 모델 생성·Blender·MPFB 실험은 삭제했다.
사용자 요청 없이 제작 파이프라인이나 실험 자료를 복원하지 않는다.

- 모델은 파일 선택 또는 URL로 읽는다. 실제 사용자 모델·렌더·다운로드 데이터는 커밋하지 않는다.
- 변경 후 `npm --prefix viewer run check`를 실행한다.
- 로딩/렌더 변경은 필요 시 실제 브라우저에서 확인한다. 표시 성공을 외형·게임 품질 합격으로 주장하지 않는다.
- 로컬 파일을 외부로 업로드하지 않는다.
